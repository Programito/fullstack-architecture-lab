package com.mesaflow.client

import org.junit.Assert.assertEquals
import org.junit.Test

/** Test trivial que valida que la infraestructura de tests unitarios funciona. */
class SmokeTest {

    @Test
    fun `la suma funciona`() {
        assertEquals(4, 2 + 2)
    }
}
